
[project]
name = "network-packet-agent-frontend"
version = "0.1.0"
dependencies = [
    "streamlit",
    "requests",
    "plotly",
    "python-dotenv"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"